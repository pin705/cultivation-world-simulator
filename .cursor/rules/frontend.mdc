---
description: 前端架构与开发规范指南
globs: web/src/**/*.{vue,ts,js}
---
# 前端架构与开发指南 (Frontend Architecture & Performance Guide)

在修改 `web/src/` 目录下的代码时，必须严格遵循以下架构规范和性能最佳实践。

## 1. 架构与数据流 (Architecture & Data Flow)
- **Server-Authoritative (后端驱动)**: 游戏状态由后端驱动。前端通过接收 `TickPayloadDTO` 来更新状态。
- **Store 职责划分**:
  - 必须遵循 Pinia Store 的职责划分。
  - `world.ts` 负责 world 级编排（初始化、tick、时间/天象状态），不应继续膨胀为 God Store。
  - **新代码优先直接使用子 Store (`map/avatar/event/ui/system`)**；`world` 的代理字段视为兼容层。
- **启动状态机**:
  - `App.vue` 应保持“装配层”职责。
  - 根层场景编排必须收敛在 `useAppShell`，避免在根组件散落多个 watcher/布尔条件分支。
  - 应用壳层必须采用 `scene + overlay` 思路：底层场景至少区分 `boot / splash / initializing / game`，`SystemMenu` 与 `LoadingOverlay` 作为 overlay 叠加。
  - 游戏主界面只能由 `scene === 'game'` 驱动，禁止通过 `!showSplash`、`!showLoading` 等反向布尔组合推断“已经进入游戏”。
  - 从 `SplashLayer` 打开的设置/读档/关于页仍属于 `splash` 上下文；打开菜单不得隐式暴露地图、状态栏或其他游戏内 UI。
  - 系统菜单应采用“壳层 + 内容层 + 流程层”拆分：`SystemMenuShell` 负责容器，`SettingsPanel` 负责设置内容，`useSystemMenuFlow` 负责菜单业务流程；不要把三者重新揉回 `SystemMenu.vue` 巨石组件。
- **Socket 分层约束**:
  - `api/socket.ts` 只负责 transport（连接、重连、消息订阅）。
  - 消息分发必须通过 `stores/socketMessageRouter.ts`，禁止在组件内直接按消息类型分支。

## 2. Vue 3 性能最佳实践 (Performance Best Practices)
- **强制约束 (MUST)**: 对于从后端接收的大型只读对象（如 `AvatarDetail`, 游戏状态快照，地图数据），**必须使用 `shallowRef`** 而不是 `ref` 或 `reactive`。这能避免深层 Proxy 转换导致的性能卡顿 (10ms-100ms+ UI freezes)。
- **长列表渲染**: 对于长列表（如事件日志、实体列表），优先使用“分页 + 增量加载”或“虚拟滚动”。当单次渲染节点数持续增长时，必须引入虚拟滚动方案。
- **Memoization**: 使用 `computed` 处理昂贵的派生状态，避免在模板中使用内联表达式或方法。
- **性能基线**: 对关键链路（初始化、事件合并/加载）保留可观测耗时指标，重构前后需比较并确保不退化。

## 3. 桌面版与浏览器适配 (Desktop & Browser)
- **强制约束 (MUST)**: 前后端共享同一套前端代码，监听画布/容器尺寸变化时，**严禁使用 `useWindowSize()`**（它会把窗口尺寸当成真源，容易与实际容器尺寸脱节）。
- **必须统一使用 `useElementSize(container)`**（基于 `ResizeObserver`），以确保尺寸变化始终以实际容器为准。

## 4. 异常恢复与渲染 (Error Recovery & Rendering)
- **防闪烁设计**: 依赖 `boot` 场景保护。在完成 settings hydrate 且收到后端 `initStatus` 第一次响应前，前端界面必须保持纯黑壳层以防闪烁。
- **错误处理统一出口**:
  - 记录日志优先使用 `utils/appError.ts`（`logError/logWarn`），避免裸 `console.error/warn` 分散在业务代码中。
  - 用户提示统一通过 `message`，避免重复弹窗或风格不一致。
- **Pixi 渲染优化**: 
  - **强制约束 (MUST)**: **严禁将 PIXI 原生对象 (如 `Sprite`, `Container`) 放入 Vue 的深层响应式对象 (`ref`, `reactive`) 中**。这会导致 PIXI 内部严格相等 (`===`) 比较失败（如 `removeChild` 失效），引发内存泄漏和逻辑 Bug。若需保存实例引用，请使用普通变量或 `shallowRef`。
  - 动态效果（如水面流动）必须使用 `PIXI.Ticker` 独立驱动，不依赖 Vue 渲染循环。
  - 地图使用 `shallowRef` 存储。地块渲染使用 `onMounted` 一次性构建 Pixi Sprite，静态地块不参与响应式更新。
  - 地图区域名布局（`MapLayer` / `utils/mapLabels.ts`）默认采用**碰撞避让**而不是“重叠即隐藏”。若必须取舍，优先保持标签可见，再通过有限候选位避让。
  - 地图区域名布局必须兼容当前启用的五种语言：`zh-CN`、`zh-TW`、`en-US`、`vi-VN`、`ja-JP`。紧凑脚本与拉丁脚本必须分别按对应文字尺寸和换行规则估算包围盒，禁止只按中文或只按英文做单语言近似。
  - 地图区域名避让实现必须保持轻量：优先使用固定候选位 + 邻域索引（如空间桶）进行局部碰撞检测，禁止引入逐帧求解、全局物理模拟或会明显增加初始化/重绘开销的重型算法。

## 5. 类型与 DTO 规范 (Type & DTO Policy)
- **新增接口字段时**，必须先更新 `types/api.ts`，再在 `api/mappers/*` 做 DTO -> Domain 归一化。
- **禁止扩散 `any`**: 新增代码中不得引入无必要 `any`；确需过渡时必须标注用途和后续收敛点。

## 6. 静态资源路径约定 (Static Asset Paths)
- **前端自带静态资源**（`web/public/*`）禁止直接写死站点根绝对路径（如 `/icons/x.png`、`/sfx/x.ogg`、`/bgm/x.mp3`）。
- 运行时拼接路径的 `public` 资源必须通过统一 helper 基于 `import.meta.env.BASE_URL` 生成 URL。
- 组件内固定图片、图标优先放入 `web/src/assets/*` 并使用模块导入，让 Vite 负责产物路径和部署前缀。
- **游戏运行资源**（后端或代理提供的 `/assets/*`）与前端 `public` 资源分开管理，不要混用目录和 URL 生成方式。

## 7. 系统级状态栏配色 (System Status Bar Colors)
- 状态栏/系统级导航颜色必须区分“模块基础色”和“状态覆盖色”；基础色负责表达栏目归属，状态色只用于激活、可用、稀有度等动态变化。
- 避免把黄橙色同时大量用于模块身份、奖励感和激活态；同一色相带若承担多个职责，必须先收敛语义再加新入口。
- 同一世界观簇的入口应共享相近色系：信息/知识偏冷蓝，关系/组织偏靛蓝，凡俗/生民偏青绿，王朝/礼制偏赭金，竞技/荣誉偏金色。
